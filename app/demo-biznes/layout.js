import BiznesLayout from "../../components/demo-biznes/BiznesLayout";

export const metadata = {
  title: "Salon Elegancja — Pakiet Biznes",
};

export default function DemoBiznesLayout({ children }) {
  return <BiznesLayout>{children}</BiznesLayout>;
}
